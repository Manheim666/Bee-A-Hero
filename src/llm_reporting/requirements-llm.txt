# LLM reporting stage (Bee-A-Hero: Data -> CV -> ML -> LLM).
# The offline template back-end needs only pandas (already a project dep). The Claude
# back-end additionally needs the official Anthropic SDK:
#
#   pip install -r src/llm_reporting/requirements-llm.txt
#
# Credentials resolve from the environment (ANTHROPIC_API_KEY or an `ant auth login`
# profile); no key is stored in the repo. Without a credential the generator falls back
# to the offline template automatically.
pandas>=2.0
anthropic>=0.40
